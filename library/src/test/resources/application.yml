spring:
  shell:
    interactive:
      enabled: false

  liquibase:
    drop-first: true

  datasource:
    url: jdbc:h2:mem:testdb
    username: sa
    password:
    driver-class-name: org.h2.Driver