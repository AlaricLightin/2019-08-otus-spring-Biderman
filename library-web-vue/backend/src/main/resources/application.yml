spring:
  liquibase:
    enabled: true

  datasource:
    url: jdbc:postgresql:otus-library-web
    username: springuser
    password: 123

  jpa:
    hibernate:
      ddl-auto: none

    properties:
      hibernate:
        jdbc:
          lob:
            non_contextual_creation: true
